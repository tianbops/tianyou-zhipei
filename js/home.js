/* 天友智配One - 首页业务逻辑 */
(function(){
'use strict';
let baseStores=[],parsedOrders=[],serverToday=null,pendingMeta={},reviewMode=false;
const $=id=>document.getElementById(id);
function toast(message,type=''){let el=$('homeToast');if(!el){el=document.createElement('div');el.id='homeToast';el.className='toast';document.body.appendChild(el)}el.textContent=message;el.className=`toast show ${type}`;clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.classList.remove('show'),2800)}
window.homeToast=toast;
function error(message){const box=$('error-box');if(!box)return;box.textContent='页面错误：'+message;box.classList.add('show');setTimeout(()=>box.classList.remove('show'),5000)}
function today(){return new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Shanghai',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())}
function storeName(store){return String(store?.name||store?.storeName||store?.shopName||'').trim()}
function currentRoute(){return Auth.getCurrentRoute()}
async function loadBaseStores(){const route=currentRoute();if(!route)throw Error('未绑定线路');const response=await fetch(`/api/routes?route=${encodeURIComponent(route)}`,{cache:'no-store',credentials:'same-origin'});if(!response.ok)throw Error(response.status===401?'登录已失效，请重新登录':`线路基准读取失败（${response.status}）`);const data=await response.json();const stores=Array.isArray(data)?data:(data?.stores||data?.data||[]);if(!Array.isArray(stores)||!stores.length)throw Error('当前线路暂无基准门店数据');baseStores=stores;return baseStores}
async function loadServerToday(date=''){const route=currentRoute();if(!route)throw Error('未指定配送线路');const params=new URLSearchParams({route});if(date)params.set('date',date);const response=await fetch(`/api/orders?${params}`,{cache:'no-store',credentials:'same-origin'});if(!response.ok)throw Error(response.status===401?'登录已失效，请重新登录':`今日订单读取失败（${response.status}）`);const data=await response.json();serverToday=data?.today||null;return serverToday}
function parseWeight(value){if(value===null||value===undefined||value==='')return 0;const text=String(value).trim().replace(/,/g,'');const match=text.match(/[\d]+(?:\.\d+)?/);if(!match)return 0;const n=Number(match[0]);if(!Number.isFinite(n))return 0;const tons=/吨|\bt\b/i.test(text)?n:n/1000;return Number.isFinite(tons)?tons:0}
function formatWeight(value){const tons=parseWeight(value);return tons>0?`${(Math.round((tons+Number.EPSILON)*10)/10).toFixed(1)}t`:'暂无数据'}
function updateSummary(){const orders=Array.isArray(serverToday?.orders)?serverToday.orders:[],count=orders.length,route=serverToday?.route||currentRoute()||'',vehicle=serverToday?.vehicle||'';if($('menuRoute'))$('menuRoute').textContent=route||'未选择线路';if($('homeRoute'))$('homeRoute').textContent=vehicle?`🚚 ${vehicle}`:`🚚 ${route}`;if($('storeCount'))$('storeCount').textContent=count?`${count}家门店`:'暂无今日订单';if($('totalWeight'))$('totalWeight').textContent=formatWeight(serverToday?.totalWeight);if($('statusDot'))$('statusDot').style.background=count?'#27AE60':'#5A6A7A'}
function renderStatus(status,count=0,message=''){const box=$('parseStatus');if(!box)return;box.classList.add('active');if($('statusIcon'))$('statusIcon').textContent=status==='success'?'✅':status==='error'?'⚠️':'⏳';if($('statusText'))$('statusText').textContent=message||(status==='success'?'处理完成':status==='error'?'处理失败':'正在处理...');if($('progressBar'))$('progressBar').style.width=status==='success'||status==='error'?'100%':'50%';if($('statusCount'))$('statusCount').textContent=count?`解析得到 ${count} 家门店`:''}
function renderTags(items){const box=$('parsedTags');if(!box)return;box.innerHTML='';items.slice(0,80).forEach((item,i)=>{const tag=document.createElement('span');tag.textContent=`${String(item.code||item.displayCode||i+1).padStart(2,'0')} ${item.isNew?'⚠️ ':''}${item.name}`;tag.style.cssText='background:rgba(36,87,166,.18);padding:4px 7px;border-radius:7px;color:#AFC7E8;font-size:10px;';box.appendChild(tag)})}
function setOCRText(text){const input=$('manualOrderInput');if(!input)return false;const value=String(text??'').replace(/\r\n/g,'\n').replace(/\r/g,'\n');input.value=value;input.removeAttribute('placeholder');input.dispatchEvent(new Event('input',{bubbles:true}));if($('charCount'))$('charCount').textContent=String(value.length);input.scrollTop=0;return !!value.trim()}
function setReviewText(data){const input=$('manualOrderInput');if(!input)return false;const stores=Array.isArray(data?.stores)?data.stores:[];const route=data?.route||currentRoute()||'';const date=data?.date||pendingMeta.date||today();const vehicle=data?.vehicle||pendingMeta.vehicle||'';const totalWeight=data?.totalWeight||pendingMeta.totalWeight||'';const lines=[`【今日订单信息】`,`日期：${date}`,`线路：${route}`,`车辆：${vehicle||'未识别'}`,`门店数：${stores.length}家`,`总重量：${totalWeight||'未识别'}`,``,`【门店列表】`];stores.forEach((item,index)=>{const prefix=String(index+1).padStart(2,'0');const mark=item?.isNew?'⚠️ 新增：':'';const review=item?.needsReview?'⚠️ 待确认：':'';lines.push(`${prefix}. ${mark||review}${storeName(item)}`)});if(!stores.length)lines.push('暂无有效门店');const value=lines.join('\n');reviewMode=true;input.value=value;input.removeAttribute('placeholder');if($('charCount'))$('charCount').textContent=String(value.length);input.scrollTop=0;return true}
async function parseOrderText(text){const route=currentRoute();if(!route)throw Error('未指定配送线路');const response=await fetch('/api/parse',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,route}),credentials:'same-origin',cache:'no-store'});const data=await response.json().catch(()=>({}));if(!response.ok||!data.success)throw Error(data.error||`解析接口错误（${response.status}）`);return data.data}
window.toggleUpload=()=>{const overlay=$('uploadOverlay');if(overlay)overlay.classList.toggle('active')};
window.openHomeMenu=()=>{const menu=$('homeMenu');if(menu)menu.style.display=menu.style.display==='block'?'none':'block'};
window.goToRouteEdit=()=>{window.location.href='pages/route_edit.html'};
window.goToOrderDetail=()=>{const route=currentRoute();window.location.href=`pages/order_detail.html${route?`?route=${encodeURIComponent(route)}`:''}`};
window.goToHistory=()=>{window.location.href='pages/history.html'};
window.logout=()=>Auth.logout();
window.clearManualInput=()=>{const input=$('manualOrderInput');if(input){input.value='';input.setAttribute('placeholder','上传图片后，这里显示识别文字。请核对后再开始解析，也可直接粘贴运单文字。')}if($('charCount'))$('charCount').textContent='0';parsedOrders=[];pendingMeta={};reviewMode=false;renderStatus('idle');renderTags([]);window.renderReviewStores?.([])};
window.loadExampleData=()=>{const names=baseStores.slice(0,3).map(storeName).filter(Boolean);setOCRText(names.length?names.join('\n'):'江北胡汪洋经销商\n中景隆贸易\n江北重庆兴农');pendingMeta={};reviewMode=false;renderStatus('idle')};
window.pasteFromClipboard=async()=>{try{const text=await navigator.clipboard.readText();setOCRText(text);pendingMeta={};reviewMode=false}catch(_){toast('无法读取剪贴板，请手动粘贴','warning')}};
window.parseManualInput=async()=>{try{const text=$('manualOrderInput')?.value||'';if(!text.trim())return toast('请先输入或识别运单文字','warning');renderStatus('loading',0,'正在提取门店并与基准库比对...');const data=await parseOrderText(text);parsedOrders=Array.isArray(data.stores)?data.stores:[];pendingMeta={date:data.date||pendingMeta.date||'',vehicle:data.vehicle||pendingMeta.vehicle||'',totalWeight:data.totalWeight||pendingMeta.totalWeight||'',rawOrderCount:data.rawOrderCount||0,matchedCount:data.matchedCount,newStoreCount:data.newStoreCount,recognizedCount:data.recognizedCount,source:'web-confirm'};renderTags(parsedOrders);window.onOrderParsed?.(data);setReviewText(data);renderStatus(parsedOrders.length?'success':'error',parsedOrders.length,parsedOrders.length?`解析完成：${parsedOrders.length} 家门店${data.newStoreCount?`，新增 ${data.newStoreCount} 家`:''}`:'没有识别到有效门店');if(data.warning)toast(data.warning,'warning');else toast('解析完成，请人工核对今日订单信息');return parsedOrders}catch(e){parsedOrders=[];reviewMode=false;window.onOrderParsed?.({stores:[]});renderStatus('error',0,e.message);toast(e.message||'解析失败','warning');error(e.message||'解析失败');return[]}};
document.addEventListener('DOMContentLoaded',async()=>{try{if(typeof Auth==='undefined')throw Error('Auth 未加载');if(!(await Auth.checkAuth()))return;await Promise.all([loadBaseStores(),loadServerToday()]);updateSummary();$('manualOrderInput')?.addEventListener('input',function(){if($('charCount'))$('charCount').textContent=String(this.value.length);if(reviewMode){reviewMode=false;parsedOrders=[];window.onOrderParsed?.({stores:[]});renderTags([]);renderStatus('idle',0,'订单信息已修改，请重新点击“开始解析”')}});document.addEventListener('click',event=>{const menu=$('homeMenu'),button=document.querySelector('.menu-btn');if(menu&&menu.style.display==='block'&&!menu.contains(event.target)&&!button?.contains(event.target))menu.style.display='none'})}catch(e){console.error(e);error(e.message||'首页初始化失败')}});
})();
