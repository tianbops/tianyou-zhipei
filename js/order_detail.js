/* 天友智配One - 今日运单详情 */
(function(){'use strict';const $=id=>document.getElementById(id);function readJSON(k,f=null){try{const v=localStorage.getItem(k);return v?JSON.parse(v):f}catch(_){return f}}function todayKey(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}function cleanName(v){return String(v||'').replace(/[\u3000]/g,' ').replace(/^\s*[\d０-９]+[、.．)）\s-]*/u,'').replace(/\s+/g,' ').trim()}function key(v){return cleanName(v).replace(/[\s，,。；;：:（）()【】\[\]]/g,'').toLowerCase()}function parseWeight(v){const m=String(v??'').replace(/,/g,'').match(/\d+(?:\.\d+)?/);if(!m)return 0;const n=Number(m[0]);return/吨|\bt\b/i.test(String(v))?n*1000:n}function normalizeStore(x,i){if(typeof x==='string')return{code:String(i+1).padStart(2,'0'),name:cleanName(x),nav:'',weight:0,isNew:false};x=x||{};return{code:String(x.code||x.index||i+1).padStart(2,'0'),name:cleanName(x.name||x.storeName||x.shopName||x['门店名称']),nav:String(x.nav||x.navigation||x.url||x.amap||x['导航']||'').trim(),weight:parseWeight(x.weight??x['重量']??0),isNew:Boolean(x.isNew||x.newStore||x.is_new)}}function normalizeOrders(raw){if(!Array.isArray(raw))return[];const seen=new Set();return raw.map(normalizeStore).filter(x=>{if(!x.name)return false;const k=key(x.name);if(seen.has(k))return false;seen.add(k);return true})}function getBaseStores(route){const local=readJSON('base_data',[]);if(Array.isArray(local)&&local.length)return local;const c=readJSON(`route_cache_${route}`,null);return Array.isArray(c?.stores)?c.stores:[]}function baseName(x){return cleanName(x?.name||x?.storeName||x?.shopName||'')}function sortByBaseRoute(raw,base){const src=normalizeOrders(raw),bs=Array.isArray(base)?base:[];if(!bs.length)return src.map((x,i)=>({...x,displayCode:String(i+1).padStart(2,'0')}));const matched=[],news=[],used=new Set();bs.forEach((b,bi)=>{const bk=key(baseName(b)),bc=String(b?.code||bi+1).padStart(2,'0');const idx=src.findIndex((o,i)=>!used.has(i)&&((bk&&key(o.name)===bk)||(!o.name&&String(o.code||'')===bc)));if(idx<0)return;used.add(idx);const o=src[idx];matched.push({...o,code:bc,displayCode:bc,name:baseName(b)||o.name,nav:String(b?.nav||b?.navigation||b?.url||b?.amap||o.nav||'').trim(),isNew:false})});src.forEach((o,i)=>{if(used.has(i))return;if(!bs.some(b=>key(baseName(b))===key(o.name)))news.push({...o,isNew:true})});news.forEach((x,i)=>x.displayCode=String(matched.length+i+1).padStart(2,'0'));return matched.concat(news)}function renderSummary(orders,totalWeightValue){const total=orders.length,n=orders.filter(x=>x.isNew).length;if($('storeCount'))$('storeCount').textContent=n?`⚠️ 新${n}家、总${total}家`:`${total}家`;const calc=orders.reduce((s,x)=>s+parseWeight(x.weight),0),w=totalWeightValue!==''&&totalWeightValue!=null?parseWeight(totalWeightValue):calc;if($('totalWeight'))$('totalWeight').textContent=`${Number(w.toFixed(2))} kg`}function renderRoute(orders,totalWeightValue=''){const box=$('routeList');if(!box)return;box.innerHTML='';if(!orders.length){box.innerHTML='<div class="empty-tip"><span class="icon">📭</span>今日暂无配送数据</div>';if($('storeCount'))$('storeCount').textContent='0家';if($('totalWeight'))$('totalWeight').textContent='0 kg';return}orders.forEach(s=>{const row=document.createElement('div');row.className='store-item';const idx=document.createElement('span');idx.className='store-index';idx.textContent=`${s.displayCode||s.code}、`;const name=document.createElement('span');name.className='store-name';name.textContent=s.isNew?'⚠️ 新增 '+s.name:s.name;const nav=document.createElement('button');nav.className='nav-btn';nav.type='button';nav.textContent='导航';nav.onclick=()=>s.nav?window.location.href=s.nav:alert('该门店暂无导航地址');row.append(idx,name,nav);box.appendChild(row)});renderSummary(orders,totalWeightValue)}async function loadOrders(route){const base=getBaseStores(route),local=readJSON('today_orders',[]),date=localStorage.getItem('today_order_date');if(Array.isArray(local)&&local.length&&(!date||date===todayKey()))return{orders:sortByBaseRoute(local,base),totalWeight:localStorage.getItem('today_total_weight')||''};try{const r=await fetch(`/api/history?date=${todayKey()}&route=${encodeURIComponent(route)}`,{cache:'no-store'});if(!r.ok)return{orders:[],totalWeight:''};const d=await r.json(),rec=Array.isArray(d)?(d.find(x=>x?.date===todayKey()&&(!x.route||x.route===route))||d[0]):d,raw=rec?.orders||rec?.today_orders||rec?.data?.orders||[],orders=sortByBaseRoute(raw,base),w=rec?.totalWeight??rec?.weight??rec?.data?.totalWeight??'';if(orders.length){localStorage.setItem('today_orders',JSON.stringify(orders));localStorage.setItem('today_order_date',todayKey());if(w!=='')localStorage.setItem('today_total_weight',String(w))}return{orders,totalWeight:w}}catch(e){console.warn('今日运单 API 读取失败:',e);return{orders:[],totalWeight:''}}}function initHeader(route,v){if($('routeName'))$('routeName').textContent=route||'未选择线路';if($('menuRoute'))$('menuRoute').textContent=route||'未选择线路';if($('todayDate'))$('todayDate').textContent=localStorage.getItem('today_order_date')||todayKey();if($('vehicleText'))$('vehicleText').textContent=v||'渝DK7692';if($('newVehicle'))$('newVehicle').value=v||'渝DK7692'}window.toggleMenu=()=>{const m=$('menuPanel');if(m)m.style.display=m.style.display==='block'?'none':'block'};window.openVehicle=()=>{if($('vehicleDialog'))$('vehicleDialog').style.display='flex';if($('menuPanel'))$('menuPanel').style.display='none'};window.closeVehicle=()=>{if($('vehicleDialog'))$('vehicleDialog').style.display='none'};window.saveVehicle=()=>{const v=$('newVehicle')?.value.trim();if(!v)return alert('请输入车辆号码');localStorage.setItem('today_vehicle',v);if($('vehicleText'))$('vehicleText').textContent=v;window.closeVehicle()};window.shareOrder=async()=>{const o=readJSON('today_orders',[]),n=o.filter(x=>x.isNew).length,t=o.length,text=`天友智配One\n${localStorage.getItem('today_order_date')||todayKey()}\n${Auth.getCurrentRoute()}\n🚚 ${localStorage.getItem('today_vehicle')||'渝DK7692'}\n${n?`⚠️ 新${n}家、总${t}家`:`${t}家`}\n总重量 ${$('totalWeight')?.textContent||'0 kg'}`;try{if(navigator.share)await navigator.share({title:'今日运单',text});else if(navigator.clipboard){await navigator.clipboard.writeText(text);alert('📋 运单信息已复制')}else alert(text)}catch(_){}};window.goBack=()=>{window.location.href='../home.html'};window.logout=()=>{if(confirm('确定退出登录吗？'))Auth.logout()};document.addEventListener('DOMContentLoaded',async()=>{if(!window.Auth||!Auth.checkAuth())return;const route=Auth.getCurrentRoute(),vehicle=localStorage.getItem('today_vehicle')||'渝DK7692';initHeader(route,vehicle);const result=await loadOrders(route);renderRoute(result.orders,result.totalWeight);setTimeout(()=>renderSummary(result.orders,result.totalWeight),350);document.addEventListener('click',e=>{const m=$('menuPanel'),b=document.querySelector('.menu-btn');if(m&&!m.contains(e.target)&&!b?.contains(e.target))m.style.display='none'})})})();